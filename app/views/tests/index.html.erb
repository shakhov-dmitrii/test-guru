<h1>Tests list</h1>
<table>
  <tr>
    <td>Test title</td>
    <td>Category</td>
    <td>Level</td>
    <td>Questions</td>
    <td>Action</td>
  </tr>
  <% @tests.each do |test| %>
    <tr>
      <td><%= test.title %></td>
      <td><%= test.category.title %></td>
      <td><%= test.level %></td>
      <td><%= test.questions.count %></td>
      <td><%= button_to 'Start', start_test_path(test) %></td>
    </tr>
  <% end %>
</table>


